import React, { useCallback } from "react";
import styled from "styled-components";

const ResizeElement = styled.div`
  ${{}}
`;
function Resizer({ x, y, dispatch }) {
  return <ResizeElement>Hello</ResizeElement>;
}

Resizer.defaultProps = {};
